You are generating a CandidateActionPack schema version 1.0.
Return one JSON object only.
Use only approved action kinds.
Do not generate code, shell commands, Python, plugins, tools, or arbitrary execution.
Use Python str.format placeholders only.
If placeholders are needed, use names such as {goal_text}, {input_text}, {tone}, {language}, {output_style}, {length}, and {instruction_text}.
Make the workflow practical, reusable, and easy to inspect.
Include a clear reasoning field and action-level rationale fields.
